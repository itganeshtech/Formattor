@model List<FormatResult.Models.SubjectWiseResultViewModel>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

<div class="card shadow-lg p-3 mb-5 bg-white rounded">
    <div class="card-header bg-primary text-white">
        <h4><i class="fas fa-trophy me-2"></i>Subject Wise Toppers</h4>
    </div>
    <div class="card-body">
        <div class="row mb-1">
            <div class="col-md-6">
                <h5 class="text-primary">🏆 Top Performers by Subject</h5>
                <p><strong>Total Subjects:</strong> @Model.Select(s => s.SubjectCode).Distinct().Count()</p>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table table-bordered table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>S.No</th>
                        <th>Subject Name</th>
                        <th>Subject Code</th>
                        <th>Roll Number</th>
                        <th>Student Name</th>
                        <th>Marks</th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int counter = 1;
                        string[] rowColors = { "bg-light", "bg-white" };
                    }
                    @foreach (var student in Model)
                    {
                        <tr class="@rowColors[counter % 2]">
                            <td>@counter</td>
                            <td class="bg-info bg-opacity-10 fw-bold text-primary">@student.SubjectName</td>
                            <td>@student.SubjectCode</td>
                            <td>@student.RollNumber</td>
                            <td>@student.Name</td>
                            <td>
                                <span class="badge rounded-pill bg-@(student.Marks >= 90 ? "success" : student.Marks >= 75 ? "info" : "warning")">
                                    @student.Marks
                                </span>
                            </td>
                        </tr>
                        counter++;
                    }
                </tbody>
            </table>
        </div>

        <!-- Performance Summary Cards -->
    </div>
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
}








@* @model List<FormatResult.Models.SubjectWiseResultViewModel>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

<div class="card shadow-lg p-3 mb-5 bg-white rounded">
    <div class="card-header bg-primary text-white">
        <h4><i class="fas fa-trophy me-2"></i>Subject Wise Toppers</h4>
    </div>
    <div class="card-body">
        <div class="row mb-4">
            <div class="col-md-6">
                <h5 class="text-primary">🏆 Top Performers by Subject</h5>
                <p><strong>Total Subjects:</strong> @Model.Select(s => s.SubjectCode).Distinct().Count()</p>
            </div>
            <div class="col-md-6">
                <div class="alert alert-secondary">
                    <h5 class="mb-2">📊 Quick Stats</h5>
                    <p class="mb-1"><strong>Highest Marks:</strong> @Model.Max(s => s.Marks)</p>
                    <p class="mb-1"><strong>Students Listed:</strong> @Model.Count</p>
                </div>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table table-bordered table-striped table-hover">
                <thead class="bg-secondary text-white">
                    <tr>
                        <th>S.No</th>
                        <th>Subject Name</th>
                        <th>Subject Code</th>
                        <th>Roll Number</th>
                        <th>Student Name</th>
                        <th>Marks</th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int counter = 1;
                    }
                    @foreach (var student in Model)
                    {
                        <tr>
                            <td>@counter</td>
                            <td>@student.SubjectName</td>
                            <td>@student.SubjectCode</td>
                            <td>@student.RollNumber</td>
                            <td>@student.Name</td>
                            <td>
                                <span class="badge bg-@(student.Marks >= 90 ? "success" : student.Marks >= 75 ? "info" : "warning")">
                                    @student.Marks
                                </span>
                            </td>
                        </tr>
                        counter++;
                    }
                </tbody>
            </table>
        </div>

        <!-- Performance Summary Cards -->
        <div class="row mt-4">
            <div class="col-md-4 mb-3">
                <div class="card bg-success text-white">
                    <div class="card-body">
                        <h5 class="card-title">90+ Marks</h5>
                        <p class="card-text display-4">@Model.Count(s => s.Marks >= 90)</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-3">
                <div class="card bg-info text-white">
                    <div class="card-body">
                        <h5 class="card-title">75-89 Marks</h5>
                        <p class="card-text display-4">@Model.Count(s => s.Marks >= 75 && s.Marks < 90)</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-3">
                <div class="card bg-warning text-dark">
                    <div class="card-body">
                        <h5 class="card-title">Below 75</h5>
                        <p class="card-text display-4">@Model.Count(s => s.Marks < 75)</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
}
 *@


















@* @model List<FormatResult.Models.SubjectWiseResultViewModel>

<div class="mt-4">
    <h3>Subject Wise Toppers:</h3>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>SubjectName</th>
                <th>Subject (Code)</th>
                <th>RollNumber</th>
                <th>Name</th>
                <th>Marks</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var student in Model)
            {
                <tr>
                    <td>@student.SubjectName</td>
                    <td>@student.SubjectCode</td>
                    <td>@student.RollNumber</td>
                    <td>@student.Name</td>
                    <td>@student.Marks</td>
                </tr>
            }
        </tbody>
    </table>
</div>

 *@









@* @model List<FormatModals.Student>

@if (Model != null && Model.Any())
{
    <div class="card shadow-lg p-3 mb-5 bg-white rounded">
        <div class="card-header bg-primary text-white">
            <h4>Student Details</h4>
        </div>
        <div class="card-body">
            <h5 class="mb-3">👤 Personal Information</h5>
            <table class="table table-bordered">
                <tbody>
                    <tr>
                        <th>Roll Number:</th>
                        <td>@Model.RollNumber </td>
                    </tr>
                    <tr>
                        <th>Name:</th>
                        <td>@Model.Name</td>
                    </tr>
                    <tr>
                        <th>Gender:</th>
                        <td>@Model.Gender</td>
                    </tr>
                    <tr>
                        <th>Overall Result:</th>
                        <td>@Model.OverallResult</td>
                    </tr>
                    <tr>
                        <th>Percentage:</th>
                        <td>@Model.Percentage.ToString("0.00")%</td>
                    </tr>
                </tbody>
            </table>

            <h5 class="mt-4 mb-3">📚 Subject Details</h5>
            @if (Model.Subjects != null && Model.Subjects.Any())
            {
                <table class="table table-bordered table-striped">
                    <thead class="bg-secondary text-white">
                        <tr>
                            <th>Subject Code</th>
                            <th>Subject Name</th>
                            <th>Marks</th>
                            <th>Grade</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var subject in Model.Subjects)
                        {
                            <tr>
                                <td>@subject.Key</td> <!-- Subject Code -->
                                <td>@subject.Value.SubjectName</td> <!-- Subject Name -->
                                <td>@subject.Value.Marks</td> <!-- Marks -->
                                <td>@subject.Value.Grade</td> <!-- Grade -->
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p class="text-danger">No subject details found.</p>
            }
        </div>
    </div>
}
else
{
    <p class="text-danger">Student data not available.</p>
}
 *@